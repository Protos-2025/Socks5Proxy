name: Continuous Benchmarking
on:
  workflow_dispatch:
  workflow_call:
  push:
    paths:
      - '**/*.c'
      - '**/*.h'
      - '**/Makefile'
    branches:
      - main
  pull_request:
    paths:
      - '**/*.c'
      - '**/*.h'
      - '**/Makefile'
    branches:
      - main

permissions: write-all

jobs:
  benchmark:
    name: Performance regression check
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v2

      - name: Run benchmark
        run: |
          echo "DEBUG=false" > .env
          echo "TRACE=false" >> .env
          docker compose up --build --abort-on-container-exit benchmark
      
      - name: Format benchmark output
        run: |
          stats=bench/results/jmeter-html-report/statistics.json
          outfile=output.txt
          mean=$(jq '."Total".meanResTime' "$stats")
          p99=$(jq '."Total".pct3ResTime' "$stats")
          min=$(jq '."Total".minResTime' "$stats")
          max=$(jq '."Total".maxResTime' "$stats")
          errorPct=$(jq '."Total".errorPct' "$stats")
          median=$(jq '."Total".medianResTime' "$stats")
          samples=$(jq '."Total".sampleCount' "$stats")
          throughput=$(jq '."Total".throughput' "$stats")
          extra=$(printf 'Samples: %s\nMedian: %s\nThroughput: %s rpm\nError Percentage: %s%%\n' "$samples" "$median" "$throughput" "$errorPct")
          jq -n --arg name1 "JMeter Mean Response Time" --arg unit1 "Milliseconds" --argjson value1 "$mean" \
            --arg name2 "JMeter P99 Response Time" --arg unit2 "Milliseconds" --argjson value2 "$p99" \
            --arg range "$min - $max ms" --arg extra "$extra" \
            '[ { name: $name1, unit: $unit1, value: $value1 }, { name: $name2, unit: $unit2, value: $value2, range: $range, extra: $extra } ]' > "$outfile"
        
      # Download previous benchmark result from cache (if exists)
      - name: Download previous benchmark data
        uses: actions/cache@v4
        with:
          path: ./cache
          key: ${{ runner.os }}-benchmark

      # Run `github-action-benchmark` action
      - name: Store benchmark result
        uses: benchmark-action/github-action-benchmark@v1
        with:
          # What benchmark tool the output.txt came from
          tool: 'customSmallerIsBetter'
          # Where the output from the benchmark tool is stored
          output-file-path: output.txt
          # Where the previous data file is stored
          external-data-json-path: ./cache/benchmark-data.json
          # Workflow will fail when an alert happens
          fail-on-alert: true
          # GitHub API token to make a commit comment
          github-token: ${{ secrets.GITHUB_TOKEN }}
          alert-threshold: '200%'
          auto-push: false
          skip-fetch-gh-pages: true
          comment-always: true
          summary-always: true

  bpftrace:
    if: false # disable due to gha flakiness
    name: bpftrace syscall check
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v2
        
      - name: Build bpftrace container
        run: docker compose build trace

      - name: Run trace
        run: |
          set +e
          docker compose down || true
          timeout 10s docker compose up --abort-on-container-exit trace
          exit_code=$?
          if [ $exit_code -eq 0 ] || [ $exit_code -eq 124 ]; then
            echo "Benchmark finished successfully or timed out (which is expected)."
            exit 0
          else
            echo "Benchmark failed with exit code $exit_code"
            exit $exit_code
          fi